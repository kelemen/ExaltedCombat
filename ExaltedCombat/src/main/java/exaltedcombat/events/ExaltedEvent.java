package exaltedcombat.events;

/**
 * The base interface for every events in ExaltedCombat. This is just a marker
 * interface (does not provide any method) to allow the Java compiler to do some
 * verification when an event is specified.
 * <P>
 * Events in ExaltedCombat are expected to be {@code enum}s and implement this
 * interface.
 *
 * @author Kelemen Attila
 */
public interface ExaltedEvent {
}
