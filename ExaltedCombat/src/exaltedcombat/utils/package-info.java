/**
 * Contains classes cannot fit into any other package but widely useful and due
 * to being very special cases, did not warrant their own package. So this can
 * be considered as an "etc" package.
 */
package exaltedcombat.utils;
